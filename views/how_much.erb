<div class="main" align="center" valign="top">
    <h4>宿題を記録する。</h4>
    <form action="/howmuch" method="post" class="form">
        <div class="form-group">
            <label>教材</label>
            <select name="robject" class="form-control"  required>
                <% @homeworks.each do |homework| %>
                    <option value="<%= homework.id %>"><%= homework.object %></option>
                <% end %>
            </select>
            <br>
        </div>
        <div class="form-group">
            <label>ページ数</label>
            <input type="text" name="rpage" placeholder="ページ数" class="form-control" required><br>
        </div>
        <div class="form-group">
            <label>記録日</label>
            <input type="date" name="date" placeholder="記録日"  id="datepicker" class="form-control" required><br>
        </div>
        <input type="submit" value="登録" class="btn btn-primary"><br>
        <a href="/usermain" class="btn btn-info">メインページに戻る</a>
    </form>
</div>
