<div class="main" align="center" valign="top">
    <h4>記録を編集しましょう。</h4>
    <form action="/renew/rc/<%= @record.id %>" method="post" class="form">
        <div class="form-group">
            <label>ページ</label>
            <input type="text" name="rpage" placeholder="ページ数" value="<%= @record.rpage %>" class="form-control" required><br>
        </div>
        <div class="form-group">
            <label>記録日</label>
            <input type="date" name="date" placeholder="記録日" value="<%= @record.date %>" id="datepicker" class="form-control" required><br>
        </div>
        <input type="submit" value="変更を保存する" class="btn btn-primary"><br>
        <a href="/usermain" class="btn btn-info">メインページに戻る</a>
    </form>
</div>
