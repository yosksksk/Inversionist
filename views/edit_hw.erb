<div class="main" align="center" valign="top">
    <h4 style="padding-bottom: 20px;"><%= @homework.object %>を編集する。</h4>
    <form action="/renew/hw/<%= @homework.id %>" method="post" style="margin: 30px;" class="form">
        <div class="form-group">
            <label>教科</label>
            <select name="category" class="form-control">
                <% @categories.each do |category| %>
                    <% if category.id == @homework.category.id %>
                        <option value="<%=category.id %>" selected><%= category.name %></option>
                    <% else %>
                        <option value="<%=category.id %>"><%= category.name %></option>
                    <% end %>
                <% end %>
            </select>
        </div>
        <div class="form-group">
            <label>教材</label>
            <input type="text" placeholder="教材" name="object" class="form-control" value="<%= @homework.object %>">
        </div>
        <div class="form-group">
            <label>ページ数</label>
            <input type="text" placeholder="ページ" name="page" class="form-control" value="<%= @homework.page %>">
        </div>
        <div class="form-group">
            <label>開始日</label>
            <input type="date" placeholder="開始日" name="start_day" class="form-control" value="<%= @homework.start_day %>"  id="datepicker">
        </div>
        <div class="form-group">
            <label>期限</label>
            <input type="date" placeholder="期限" name="deadline" class="form-control" value="<%= @homework.deadline %>"  id="datepicker">
        </div>
        <br>
        <input type="submit" value="変更を保存する" class="btn btn-primary"><br>
        <a href="/usermain" class="btn btn-info" style="margin: 20px;">メインページに戻る</a>
    </form>
</div>
